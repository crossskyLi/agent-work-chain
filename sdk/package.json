{
  "name": "@agent-work-chain/sdk",
  "version": "0.2.0",
  "description": "SDK for interacting with the AuditRegistry on-chain protocol — audit submission, trust scores, certifications",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "jest"
  },
  "dependencies": {
    "ethers": "^6.13.0",
    "pinata": "^1.5.0"
  },
  "devDependencies": {
    "jest": "^29.0.0",
    "typescript": "^5.6.0"
  },
  "license": "MIT"
}
